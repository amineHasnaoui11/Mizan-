import { FileText } from "lucide-react";
import { Placeholder } from "@/components/layout/Placeholder";

export function DevoirsPage() {
  return <Placeholder titleKey="devoirs.title" subtitleKey="devoirs.subtitle" icon={FileText} />;
}
